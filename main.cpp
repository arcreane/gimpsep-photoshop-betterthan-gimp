#include <iostream>
#include "OpenCV.h"

int main(int, char**) {
    String folderLocation;
    cout << "Enter the image folder path\n";
    cin >> folderLocation;

    OpenCV opencv;
    /*opencv.setDataPath(folderLocation);

    cout << "Name of your image ?\n";
    String imageName;
    cin >> imageName;

    opencv.setImage(imageName);
    opencv.displayImage("Test");*/
}
